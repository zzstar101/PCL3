# PCL3

PCL3 is an experimental next-generation architecture for Plain Craft Launcher.

The repository is being bootstrapped around four constraints:

- cross-platform launcher core first;
- Windows, macOS and Linux as explicit platform targets;
- managed C# implementation is always available;
- native code is an optional accelerator, never a hard runtime dependency.

> This repository is in the initial architecture/bootstrap stage.
